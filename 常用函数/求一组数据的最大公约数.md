# 求一组数据的最大公约数

```js
// 最大公约数
    function getGCD(arr) {
        function gcd(a, b) {
            if (b === 0) return a
            return gcd(b, a % b)
        }
        while (arr.length > 1) {
            arr.splice(0, 2, gcd(arr[0], arr[1]))
        }
        return arr[0]
    }
```