# create-react-app



1. 创建项目

   ```shell
   npx create-react-app projectName
   
   // 支持 ts
   npx create-react-app my-app --template typescript
   ```

2. 然后提交

3. 运行命令:

   ```shell
   npm run eject
   ```

4. 